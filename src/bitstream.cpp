#include <stdio.h>
#include <stdint.h>
#include <string.h>
#include <assert.h>

#include "bitstream.h"

size_t BitStream::append(const uint8_t * src, const size_t len)
{
    // TODO: implement
    return len;
}

bool test_bitstream()
{
    return true;
}
